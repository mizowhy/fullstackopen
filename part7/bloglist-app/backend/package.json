{
  "name": "bloglist",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "test": "cross-env NODE_ENV=test node --test --test-concurrency=1",
    "test:blog": "cross-env NODE_ENV=test node --test tests/blog_api.test.js",
    "test:user": "cross-env NODE_ENV=test node --test tests/user_api.test.js",
    "test:helper": "node --test tests/list_helper.test.js",
    "start:test": "cross-env NODE_ENV=test node --watch index.js",
    "build:ui": "cd ../frontend && npm install && npm run build && Copy-Item -Path dist -Destination ../backend/dist -Recurse -Force",
    "deploy:full": "npm run build:ui && npm start",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "lodash": "^4.18.1",
    "mongoose": "^9.6.2"
  },
  "devDependencies": {
    "cross-env": "^10.1.0",
    "eslint-config-prettier": "^10.1.8",
    "prettier": "^3.8.3",
    "supertest": "^7.2.2"
  }
}
